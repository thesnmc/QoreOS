[build]
target = "x86_64-edgecore.json"

[unstable]
# Since we have no standard library, we must force Cargo to recompile the 
# absolute core Rust types (like integers and pointers) specifically for our custom target.
build-std = ["core", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]